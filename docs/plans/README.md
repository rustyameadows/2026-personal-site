# Plans

Store execution plans here when work is complex, risky, or likely to span more than one session.

Use the template and rules in `../../PLANS.md`.

Naming convention:

```text
YYYY-MM-DD-short-task-name.md
```

Do not create a plan for small, single-pass edits unless the user asks for one.
